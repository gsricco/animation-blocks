import styled from "styled-components";
import {baseTheme} from "../../common/styles/Them.styled";

export const WrapperMain = styled.div`
display: flex;
    flex-wrap: wrap;
    background: ${baseTheme.colors.backgroundPage.pageSelect};
    width: 1234px;
    height: fit-content;

`
